export default {
    "eslintConfig": {
    "extends": ["react-app", "shared-config"],
        "rules": {
    },
    "overrides": [
        {
            "files": ["**/*.ts?(x)"],
            "rules": {
            }
        }
    ]
}
}